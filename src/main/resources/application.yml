spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/fftl_lookupBoard?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: root

  #JPA 정보
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: create

